# 多模态 AI 对话应用 · 设计文档

> 一款基于国产模型的端云协同多模态对话应用。用户通过浏览器打开摄像头与麦克风，AI 实时看到画面内容、听到用户说的话，并以语音 + 文字形式给予回应。

---

## 1. 用户故事（User Stories）

### 1.1 计划实现的用户故事

| ID | 用户故事 | 优先级 | 实现状态 | 当前说明 |
|----|---------|--------|----------|----------|
| US-01 | 作为用户，我希望在浏览器中打开页面就能直接使用摄像头和麦克风，无需安装任何软件 | P0 | ✅ 已实现 | Web 端支持摄像头、麦克风、文字输入和语音播报；手机或生产环境需要 HTTPS |
| US-02 | 作为用户，我希望对着麦克风说话时，AI 能够理解我在说什么 | P0 | ✅ 已实现 | 使用 Web Speech API / Capacitor Speech Recognition，支持静音后自动发送 |
| US-03 | 作为用户，我希望 AI 能够“看到”摄像头中的画面，理解场景内容 | P0 | ✅ 已实现 | 用户发送文字或语音消息时自动抓取当前帧作为视觉输入 |
| US-04 | 作为用户，我希望 AI 用中文语音回复我，同时可以看到文字内容 | P0 | ✅ 已实现 | 后端 SSE 流式返回文字，前端按句播报，TTS 支持浏览器/原生/可选服务端 |
| US-05 | 作为用户，我希望看到视频画面实时显示在页面上，确认摄像头正常工作 | P1 | ✅ 已实现 | 摄像头模块支持实时预览、启用和停用 |
| US-06 | 作为用户，我希望看到完整的对话历史，方便回溯 | P1 | ✅ 已实现 | 对话区展示用户、AI、系统消息和每轮 token 拆解 |
| US-07 | 作为用户，我希望可以控制 AI 何时接收画面输入 | P1 | ✅ 已实现 | 已取消手动画面传输按钮，改为发送消息时自动取帧；关闭摄像头后不携带旧图 |
| US-08 | 作为用户，我希望可以调节图像分辨率、图片质量和抽帧频率 | P2 | ✅ 已实现 | 画面设置面板支持分辨率、质量上下限、抽帧频率和自适应质量 |
| US-09 | 作为用户，我希望能看到当前对话消耗的 token 和估算费用 | P2 | ✅ 已实现 | 状态栏展示调用次数、输入/输出 token、总 token、节省 token 和估算费用 |
| US-10 | 作为用户，我希望可以一键清空对话或重置会话 | P2 | ✅ 已实现 | 对话模块提供清空历史和重置会话入口 |
| US-11 | 作为用户，我希望有一个 iOS 版本，可以在手机上使用 | P3 | ⚠️ 部分具备 | 已引入 Capacitor 相关依赖与平台适配，但尚未交付可发布的 iOS App |
| US-12 | 作为用户，我希望长对话不会因为历史过长而成本过高 | P1 | ✅ 已实现 | 历史 token 达到阈值后自动摘要，并统计节省 token |
| US-13 | 作为用户，我希望能一键切换不同场景参数 | P1 | ✅ 已实现 | 支持日常对话、看图讲解、文本识别/讲题、绘本陪读、学习辅导等预设 |
| US-14 | 作为用户，我希望提问时 AI 总能看到最新画面 | P0 | ✅ 已实现 | 文本发送和语音句子结束时都会重新抓取当前帧 |
| US-15 | 作为用户，我希望知道每轮实际发送给 AI 的画面 | P1 | ✅ 已实现 | 带图用户消息旁展示缩略图，点击可全屏查看，再点击任意位置关闭 |
| US-16 | 作为用户，我希望可以选择省钱、均衡、高清识别等模式 | P1 | ✅ 已实现 | 质量模式联动抽帧频率、分辨率、质量范围和摘要阈值 |
| US-17 | 作为用户，我希望看图讲解、文本识别、操作指导有专门场景 | P2 | ✅ 已实现 | 已新增多种场景预设，并联动提示词与图像参数 |
| US-18 | 作为用户，我希望语音消息发出后录音状态明确关闭 | P2 | ✅ 已实现 | 语音句子发送后停止 ASR，AI 播报期间禁用录音并过滤迟到识别事件 |
| US-19 | 作为用户，我希望 AI 播报更连贯 | P2 | ✅ 已实现 | 回复按句切分播报，并合并过短片段减少割裂感 |
| US-20 | 作为用户，我希望系统能自动判断我是否在说话并记录为输入 | P3 | ⚠️ 部分实现 | 当前使用静音检测结束一句话；未接入独立 VAD 模型 |
| US-21 | 作为用户，我希望 AI 能理解简单动作变化 | P3 | ❌ 暂未实现 | 当前只发送单帧图片；多帧动作理解仍处于评估阶段 |

### 1.2 实际实现情况

- **已实现**：US-01 ~ US-10、US-12 ~ US-19。Web 端已经覆盖摄像头、语音输入、多模态问答、流式回复、语音播报、发送帧缩略图、token 统计、对话摘要、质量模式和场景预设。
- **部分具备**：US-11、US-20。项目已引入 Capacitor 相关能力，但还没有交付 iOS 安装包；语音输入使用静音检测完成自动发送，但没有独立 VAD 模型。
- **暂未实现**：US-21。动作理解需要多帧上下文或视频片段输入，会显著增加图像 token，当前默认不启用。

### 1.3 已实现故事的使用场景

| 场景 | 涉及故事 | 说明 |
|------|---------|------|
| 远程教孩子读绘本 | US-01, US-02, US-03, US-04, US-05 | 摄像头对准绘本，用户朗读/提问，AI 看到画面后解释内容 |
| 烹饪时问步骤 | US-01, US-02, US-04, US-06 | 用户口头问"下一步做什么"，AI 语音回答并在历史中留存文本 |
| 学习外语 | US-01, US-02, US-04, US-06 | 用户用中文描述画面，AI 用中文回复（后续可扩展外语 TTS） |
| 识别物体/植物 | US-01, US-03, US-04, US-08 | 摄像头对准物体，问"这是什么"，AI 回答 |

---

## 2. 系统架构

### 2.1 总体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        浏览器（Client）                          │
│                                                                  │
│  ┌──────────────┐  抽帧   ┌──────────────┐  base64 JPEG         │
│  │ 🎥 摄像头     │ ─────→ │ 智能抽帧模块   │ ─────────────┐       │
│  └──────────────┘         │ (帧差检测+压缩) │              ↓       │
│                           └──────────────┘  ┌──────────────────┐  │
│  ┌──────────────┐                           │                  │  │
│  │ 🎤 麦克风     │ ───────────────────────→ │  多模态消息总线  │  │
│  └──────────────┘  实时音频流               │  (MediaStream)   │  │
│         │                                   │                  │  │
│         │ ↓ Web Speech / Capacitor ASR        │                  │  │
│         │  浏览器/原生语音识别                │                  │  │
│         └─→ 识别后的文本 ─────────────────→ │                  │  │
│                                             │         │        │  │
│                                             └────┬────┘        │  │
│                                                  │ ↓ POST      │  │
│  ┌──────────────────────┐                   ┌───┴───┐         │  │
│  │ 🔊 语音播报           │ ←─ 文本分句 ───── │  SSE  │         │  │
│  │ (TTS fallback)       │                   │ Stream│         │  │
│  └──────────────────────┘   文本历史 ←───── │       │         │  │
│  ┌──────────────────────┐                   └───────┘         │  │
│  │ 💬 对话 UI (React)    │                                     │  │
│  │ + 视频预览            │                                     │  │
│  │ + 抽帧频率/成本统计   │                                     │  │
│  └──────────────────────┘                                     │
└─────────────────────────────────────────────────────────────────┘
                              │
                          HTTP(S) POST /api/multimodal/stream
                              │
┌─────────────────────────────────────────────────────────────────┐
│                      后端（Node.js Server）                       │
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  核心控制器 (ConversationController)                       │  │
│  │  - 维护用户会话 (sessionId → 对话上下文)                    │  │
│  │  - 接收图像 + 用户文本 + 历史对话                            │  │
│  │  - 组装 Prompt 发送给豆包多模态                             │  │
│  │  - 接收 AI 文本回复 → 调用 TTS → 音频流返回                   │  │
│  │  - 成本追踪（token/调用次数）                                │  │
│  └───────────────────────────────────────────────────────────┘  │
│          │                       │                    │         │
│          ↓ 视觉+对话              ↓ TTS               ↓ 成本日志  │
│  ┌───────────────┐        ┌──────────────┐     ┌──────────┐      │
│  │ 豆包多模态 API │        │ 火山引擎 TTS │     │ 文件日志 │      │
│  │ (Doubao VL)   │        │ API          │     │ (可选)   │      │
│  └───────────────┘        └──────────────┘     └──────────┘      │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 关键数据流

1. **用户启用摄像头/语音输入** → 浏览器请求摄像头和麦克风权限
2. **视频流** → Canvas 按设置抽帧 → 压缩为 JPEG → 更新本地预览缩略图
3. **语音输入** → Web Speech API / Capacitor Speech Recognition 识别为中文文本 → 回填输入框
4. **用户发送文本或语音句子结束** → 前端重新抓取最新摄像头帧，后端收到 `{ image, userText, sessionId, settings }`
5. **后端处理** → 按会话历史和摘要状态组装 Prompt → 调用豆包多模态 API
6. **AI 文本** → 后端通过 SSE 返回 delta、summary、done 等事件
7. **浏览器** → 增量显示文字、保存发送帧缩略图、按句加入播报队列
8. **成本统计** → 后端累计 usage，前端状态栏展示 token、节省 token 和估算费用

### 2.3 技术选型

| 层 | 技术 | 说明 |
|----|------|------|
| 前端 | Vite + React + TypeScript | 轻量快速，热更新友好 |
| 前端视频 | `navigator.mediaDevices.getUserMedia` | Web 标准 API |
| 前端语音识别 | Web Speech API / Capacitor Speech Recognition | 浏览器或原生能力，不调用云端 ASR |
| 后端 | Node.js + Express + TypeScript | 简洁，JS/TS 全栈统一 |
| 视觉理解 | 豆包多模态 API（火山引擎 ark/v1 接口） | 国产，图像+文本多模态 |
| 对话推理 | 豆包大模型（同 API，多模态统一调用） | 国产，中文理解优秀 |
| 语音合成 | Web Speech Synthesis / Capacitor Text to Speech / 可选火山引擎 TTS | 浏览器或原生优先，服务端 TTS 可选 |
| 会话管理 | 内存 Map（MVP）/ Redis（生产） | MVP 简洁优先 |

---

## 3. 成本控制策略

### 3.1 成本控制技巧实现状态

| 编号 | 技巧 | 当前状态 | 实现位置 | 说明 |
|------|------|----------|----------|------|
| C-01 | 浏览器侧语音识别替代云端 ASR | ✅ 已采用 | `frontend/src/hooks/useASR.ts` | 使用 Web Speech API / Capacitor Speech Recognition，不把音频上传到云端 ASR |
| C-02 | 抽帧替代连续视频流 | ✅ 已采用 | `frontend/src/video/frameSampler.ts` | 前端只发送 JPEG 图片帧，不上传连续视频 |
| C-03 | 画面变化检测 | ✅ 已采用 | `frontend/src/video/frameSampler.ts` | 使用采样差异、变化像素比例、边缘变化和局部块变化判断画面变化 |
| C-04 | 图片尺寸控制 | ✅ 已采用 | `frontend/src/video/frameSampler.ts` | 通过最长边限制降低图片体积和图像 token |
| C-05 | JPEG 质量控制 | ✅ 已采用 | `frontend/src/video/frameSampler.ts`、`frontend/src/components/VideoPreview.tsx` | 支持图片质量上下限配置 |
| C-06 | 图片质量智能自适应 | ✅ 已采用 | `frontend/src/video/frameSampler.ts` | 根据 Laplacian 方差和边缘比例判断复杂度，动态选择 quality |
| C-07 | 本地预览帧与发送帧分离 | ✅ 已采用 | `frontend/src/App.tsx`、`frontend/src/components/ChatHistory.tsx` | 右下角缩略图仅作本地预览；发送消息时重新抓取当前帧，用户消息旁展示实际发送帧 |
| C-08 | 对话摘要 | ✅ 已采用 | `backend/src/services/conversation.ts` | 历史 token 达到阈值后生成摘要并替换旧上下文 |
| C-09 | 摘要阈值可配置 | ✅ 已采用 | `frontend/src/components/ChatHistory.tsx` | 用户可在 1K ~ 16K token 范围内调节触发阈值 |
| C-10 | token 与费用状态栏 | ✅ 已采用 | `frontend/src/components/CostStats.tsx`、`backend/src/services/conversation.ts` | 展示调用次数、输入/输出 token、总 token、节省 token 和估算费用 |
| C-11 | 输入锁定与防重复发送 | ✅ 已采用 | `frontend/src/hooks/useConversation.ts`、`frontend/src/components/ChatHistory.tsx` | 流式生成和 AI 播报期间限制输入行为，减少重复请求 |
| C-12 | 场景预设 | ✅ 已采用 | `frontend/src/presets/scenePresets.ts` | 不同任务使用不同提示词、图像参数和摘要策略 |
| C-13 | 发送即取帧 | ✅ 已采用 | `frontend/src/App.tsx`、`frontend/src/hooks/useConversation.ts` | 文本发送或语音句子结束时抓取最新帧，不需要持续上传 |
| C-14 | 成本/质量模式 | ✅ 已采用 | `frontend/src/presets/qualityModes.ts` | 省钱、均衡、高清识别三档联动视觉与摘要参数 |
| C-15 | 最大输出 token 限制 | ✅ 已采用 | `backend/src/services/doubao.ts` | 请求模型时设置 `max_tokens`，避免回复过长 |
| C-16 | 会话自动超时清理 | ✅ 已采用 | `backend/src/services/conversation.ts` | 长时间无活动的内存会话会被清理 |
| C-17 | 优化 system prompt | ✅ 已采用 | `frontend/src/presets/scenePresets.ts`、`backend/src/services/conversation.ts` | 场景化提示词减少无关上下文，并约束回答风格 |
| C-18 | 按句播报与短句合并 | ✅ 已采用 | `frontend/src/hooks/useConversation.ts` | 提升播报连贯性，减少碎片化 TTS 调用 |
| C-19 | 多帧上下文 | ❌ 暂未采用 | 暂无 | 有助于动作理解，但会明显增加图像 token |
| C-20 | 模型降级路由 | ❌ 暂未采用 | 暂无 | 需要接入多模型和任务分类逻辑，当前先使用统一多模态模型 |
| C-21 | 相似画面缓存 | ❌ 暂未采用 | 暂无 | 需要缓存命中策略和视觉描述复用策略，等待真实使用数据后再判断 |
| C-22 | 埋点驱动成本分析 | ⚠️ 已设计，暂未接入 | `docs/product-prd.md` | 已设计事件口径，当前没有接入采集 SDK |

### 3.2 成本估算（基于实现配置）

| 场景 | 每小时调用次数 | 图像 token 估算 | 对话 token 估算 | TTS 字符数 | 估算费用 / 小时 |
|------|--------------|---------------|---------------|----------|-----------------|
| 低活跃（偶尔说话） | 10-15 次 | ~150-300K | ~20-50K | ~3-5K 字 | ¥0.3 - ¥0.8 |
| 中等活跃（正常对话） | 30-50 次 | ~450-900K | ~80-150K | ~10-20K 字 | ¥1.0 - ¥2.5 |
| 高活跃（连续提问） | 60-100 次 | ~900-1800K | ~200-400K | ~30-50K 字 | ¥2.5 - ¥5.0 |

> 估算基于：豆包多模态图像输入约 ¥0.003/次，对话约 ¥0.01/1K tokens，TTS 约 ¥0.02/万字。实际以火山引擎官方定价为准。

---

## 4. API 设计

### 4.1 核心接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/multimodal/stream` | 多模态流式对话（图像 + 文本 + 历史 + SSE 增量返回） |
| POST | `/api/multimodal` | 多模态非流式对话（保留兼容） |
| POST | `/api/clear` | 清空指定会话历史 |
| GET | `/api/health` | 健康检查 |

### 4.2 POST /api/multimodal

**请求体：**

```json
{
  "sessionId": "uuid-string",
  "image": "base64-encoded-jpeg (可选)",
  "userText": "用户说的话（经过 ASR 识别后）",
  "settings": {
    "frameIntervalMs": 3000,
    "imageQuality": 0.7,
    "imageSize": 512
  }
}
```

**响应体：**

```json
{
  "aiText": "AI 的文本回复",
  "audioData": "base64-encoded-mp3",
  "cost": {
    "imageTokens": 1200,
    "textTokens": 450,
    "totalCalls": 15,
    "estimatedCost": 0.023
  }
}
```

---

## 5. 部署与运行

### 5.1 环境变量

| 变量 | 说明 |
|------|------|
| `ARK_API_KEY` | 火山引擎方舟 API Key（豆包多模态 + 对话）|
| `ARK_MODEL_ENDPOINT` | 豆包多模态模型 Endpoint ID |
| `VOLC_TTS_APP_ID` | 火山引擎 TTS App ID（可选，不填则用浏览器 TTS 回退）|
| `VOLC_TTS_ACCESS_TOKEN` | 火山引擎 TTS Access Token（可选）|
| `SERVER_PORT` | 后端端口，默认 3001 |

### 5.2 启动方式

```bash
# 1. 安装依赖
cd backend && npm install
cd ../frontend && npm install

# 2. 配置环境变量（见 .env.example）

# 3. 启动后端
cd backend && npm run dev

# 4. 启动前端（另一个终端）
cd frontend && npm run dev

# 5. 浏览器打开 http://localhost:5173
```

### 5.3 生产部署建议

- 前端：Vercel / Netlify 静态部署，或任意 CDN
- 后端：Docker + 任意 Node.js 主机（需 HTTPS，因为 getUserMedia 仅在 HTTPS/localhost 工作）
- 会话管理：内存 Map → Redis（多实例部署必选）

---

## 6. 安全与隐私

- **API Key 保护**：所有云 API Key 仅在后端环境变量中，前端不可见
- **数据传输**：生产环境必须 HTTPS / WSS
- **本地语音识别**：音频数据不离开浏览器，隐私友好
- **图像数据**：仅在用户说话瞬间的那一帧发送，不持续上传视频流
- **日志**：可选关闭图像内容日志，仅记录调用次数和 token 数

---

## 7. 后续扩展方向

- [ ] iOS 版本（Capacitor 或 SwiftUI 原生）
- [ ] 多语言 TTS 支持
- [ ] 视频流而非抽帧（WebRTC + 云端 GPU 推理）
- [ ] 用户账号体系与用量计费
- [ ] 端侧部署视觉模型（如 Qwen-VL-Lite 的 WASM 版本）进一步降本
