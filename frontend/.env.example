# 后端 API 地址。
#
# - 开发模式（桌面浏览器，vite dev server 本地运行）：留空即可，
#   默认走 `/api`，由 vite dev proxy 转发到 http://localhost:3001。
#
# - 手机浏览器 / Capacitor 原生 App：必须填写可从手机访问的地址。
#   例如：VITE_API_BASE_URL=https://192.168.1.10:3001/api
#
# - 注意：摄像头 / 麦克风 getUserMedia 只在 `localhost` 或 `https://`
#   环境下可用。手机端请务必通过 HTTPS 访问后端，否则无法使用图像与语音。
VITE_API_BASE_URL=
