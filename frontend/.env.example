# ====================================================================
# AI Talking 前端配置说明
# ====================================================================
#
# 三种运行模式（按优先级生效）：
#
#   1) 本地原生模式（默认，真正开箱即用 —— 手机装好即开）
#      需要：VITE_ARK_API_KEY 和 VITE_ARK_MODEL_ENDPOINT
#      行为：直接从设备向豆包 Ark API 发请求（Capacitor HTTP 绕开 CORS）
#
#   2) 远程后端模式
#      需要：VITE_API_BASE_URL（例如 https://192.168.1.10:3001/api）
#      行为：把请求转发到自建后端
#
#   3) 开发代理模式
#      留空即可；Vite dev server 把 /api/* 代理到 http://localhost:3001
#
# 注：
#   - 如果你是在原生 App 内运行，也可以直接在界面上「⚙ 配置 API Key」输入，
#     无需修改本文件。界面配置优先于 .env。
# ====================================================================

# ---------- 模式 1：本地原生（推荐给 App 版本） ----------
# 火山引擎 Ark - 豆包多模态 API Key
VITE_ARK_API_KEY=

# 火山引擎 Ark - 豆包多模态 Endpoint ID（或 model endpoint 字符串）
VITE_ARK_MODEL_ENDPOINT=

# ---------- 模式 2 / 3：远程后端 / 开发代理 ----------
# 留空 = 使用模式 3（Vite dev proxy）
# 填写 = 使用模式 2（手机浏览器直接访问后端）
# 示例：VITE_API_BASE_URL=https://192.168.1.10:3001/api
VITE_API_BASE_URL=
